# Hi-Tech Institute Admin Backend Environment Configuration

# Server Configuration
PORT=8000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/hitech-institute

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here-change-in-production
JWT_EXPIRE=24h

# Password Hashing Configuration
BCRYPT_SALT_ROUNDS=12

# File Upload Configuration
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/jpg,image/png,image/webp,application/pdf

# CORS Configuration
CORS_ORIGIN=http://localhost:3000,http://localhost:5000

# Security Notes:
# 1. Change JWT_SECRET to a strong, unique value in production
# 2. Use a secure MongoDB connection string with authentication
# 3. Consider using environment-specific configurations
# 4. Keep this file secure and never commit it to version control

# Production Environment Variables:
# For production deployment, ensure all sensitive values are properly secured
# and use a proper secret management system.

# Example Production Values:
# NODE_ENV=production
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/hitech-institute?retryWrites=true&w=majority
# JWT_SECRET=your-256-bit-secret-key-generated-with-crypto
# CORS_ORIGIN=https://yourdomain.com,https://admin.yourdomain.com
